rtk_test_recorder:
  ros__parameters:
    port: /dev/ttyACM0
    baud_rate: 115200
    frame_id: rtk
    log_directory: ./data
    publish_heading: true
    heading_offset_deg: 0.0
    # The receiver's correction output period. Used only by offline status analysis.
    correction_period_sec: 1.0
    line_timeout_sec: 2.0
